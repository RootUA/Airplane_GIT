В алфавітному порядку:
Володя {
			модулі Rocket, Gun (дані приватні, доступ через гетери/сетери);
			модуль Wheel (масив дані приватні, доступ через гетери/сетери);
} 
Олеся {
			головний файл з методами;
			константи;
}
Ігор {
			клас Target (дані приватні/рандомні, доступ через гетери/сетери, стрічка Name, int distance, int armor);
			конфігурація App.java;
			інтерфейс Invisiable (мені логіка не позволяє метод "інвіз" запхати в "літаючий", може в нас якийсь СУ-ХХ 80х років буде вилітати);
}
Юра {
			модулі Box, Engine, (дані приватні, доступ через гетери/сетери);
			інтерфейс Flyable з методами для головного файлу;
}
/////